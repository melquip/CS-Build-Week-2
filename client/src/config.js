const gridSize = 24;
const theme = {
  fonts: {
    primary: "'Roboto Mono', monospace, sans-serif",
  },
  colors: {
    white: '#fff',
    black: '#222',
    gray: '#ccc',
  },
  gridSize
}

export default {
  gridSize,
  theme
}
